# Development / CI tooling (not needed to run the app).
-r requirements.txt
pytest==9.1.1
ruff==0.15.8
httpx==0.28.1    # required by fastapi.testclient.TestClient
Pillow==12.3.0   # used only by the one-off static/creator.jpg resize step
