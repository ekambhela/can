# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# The trained model is committed (artifacts/model.joblib, metrics.json) so the
# Docker image needs no training step. Only the regenerable datasets are ignored.
artifacts/dataset_*.csv

# OS / editor
.DS_Store
.idea/
.vscode/
